-- 1. Add the new column to orders
ALTER TABLE public.orders
ADD COLUMN user_id character varying(255);

ALTER TABLE public.orders
ADD COLUMN printed INTEGER;

ALTER TABLE public.orders
ADD COLUMN printed_count INTEGER;

-- 2. Add the foreign key constraint
ALTER TABLE public.orders
ADD CONSTRAINT orders_user_id_fkey
FOREIGN KEY (user_id)
REFERENCES public.user_settings (user_id)
ON UPDATE NO ACTION
ON DELETE NO ACTION;
